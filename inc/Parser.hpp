#ifndef PARSER_HPP
# define PARSER_HPP

#include "Lexer.hpp"

class Parser {
public:
	Parser( void );
	~Parser( void );
	Parser( Parser const & );
	Parser	& operator=( Parser const & );
private:
};

#endif
